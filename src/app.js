class Commonly {

  constructor (){
    // Window
    this.getWidth = require('./Window/getWidth')
    this.getHeight = require('./Window/getHeight')
    // Array
    this.average = require('./Array/average')
    this.bubbling = require('./Array/bubbling')
    this.uniq = require('./Array/uniq')
    
    // Date 
    this.getDate = require('./Date/getDate')

    // DOM
    this.hide = require('./DOM/hide')
    this.show = require('./DOM/show')

    // URL
    this.urlSplic = require('./URL/urlSplic')

    // Number
    this.randows = require('./Number/randows')
    
    //
  }
}